#include"fileio.h"
#include"pointandpolygon.h"


int main() {
  vector<Polygon> areas;
  Grid grid(0, 0, nullptr);// row number ,column number, the pointer to grid coordinates. 
  vector<ValueGrid> value_grids;
  initProtectedArea(areas);

  int test;
  std::cin >> test;
  return 0;
}